---
paths:
  - "**/*.go"
  - "**/*.cs"
  - "**/*.{js,jsx,mjs,cjs}"
  - "**/*.{ts,tsx,mts,cts}"
  - "**/*.{py,pyi}"
  - "**/*.{kt,kts}"
---

# 언어 공통 작업 기준

## 적용 범위
- 프로젝트의 `CLAUDE.md`, formatter, linter, compiler, test 설정이 있으면 그 기준을 우선한다.
- 여러 언어가 함께 바뀌면 각 언어 기준을 해당 파일에만 적용하고, 공통 변경 판단은 현재 프로젝트 관례를 우선한다.

## 공통 기준
- 공개 API, exported type, 컴포넌트 props, 직렬화 형태 변경은 호출부·테스트·문서 영향을 함께 확인한다.
- 오류, null, 비동기, 취소, 리소스 소유권처럼 빠뜨리면 결함으로 이어지는 경계를 우선 확인한다.
- 리뷰 의견은 수정 가능한 구체 지점에 연결하고, 선호 차이만 있는 의견은 실제 유지보수 위험이 있을 때만 남긴다.

## 주석 작성 기준
- 주석은 다음 사람이 코드를 잘못 고치는 것을 막는 데 쓴다. 남길지 지울지 애매하면 "이 주석이 없으면 무엇을 잘못할
  수 있는가"를 묻고, 구체적인 답이 없을 때만 지운다. 주변 코드에 주석이 없다는 이유로는 건너뛰지 않는다.
- 코드로 읽히는 것이 아니라 코드로 드러나지 않는 것을 적는다 — 시그니처에 없는 제약, 외부 시스템의 뜻밖의 동작,
  실패로 끊을지 기본값으로 넘길지 같은 선택, 의도적으로 비정상으로 만든 fixture. 걸리는 코드 옆에 1~2줄로 둔다.
- 같은 제약을 doc 주석과 본문 주석처럼 겹치는 자리에 두 번 적지 않는다. 독립된 자리에서 각각 필요하면 중복으로 보지 않는다.
- 변경 경위·계획 식별자 같은 작업 과정 기록은 남기지 않고, 로직이 바뀌면 관련 주석도 같은 응답에서 고치거나 지운다.
